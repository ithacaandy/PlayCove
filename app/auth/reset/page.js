"use client";import{useState}from"react";import{createClient}from"../../lib/supabase";export default function Reset(){const s=createClient();const[pw,setPw]=useState("");const[msg,setMsg]=useState("");const go=async()=>{const{error}=await s.auth.updateUser({password:pw});setMsg(error?error.message:"Password updated, you can close this tab.")};return(<main className="container py-6 max-w-md grid gap-3"><h2 className="text-lg font-semibold">Set a new password</h2><input type="password" value={pw} onChange={e=>setPw(e.target.value)} className="input"/><button onClick={go} className="btn btn-primary">Update</button><p className="text-sm text-gray-600">{msg}</p></main>);}